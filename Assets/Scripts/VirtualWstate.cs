using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//needs a practical solution to be authored, probably a scriptable object that contains the initial data, but it needs a custom inspector to work
public class VirtualWorldState
{
    List<VirtualCharacter> characters;
    //add external events, if we end up making them
}
